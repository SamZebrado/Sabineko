# Development Log

## 2026-03-12
- forked the private pipeline into the public-facing `sabineko` copy
- removed project-specific naming and paper-specific examples from the main docs
- replaced vendor-specific role names with `NorthNo1` and `DeepSea`
- converted the NorthNo1 communication layer from browser-specific messaging to a generic HTTP API adapter
- kept browser automation as an optional integration path rather than a required core assumption
- prepared the repo for GPL-style licensing and later GitHub publication
